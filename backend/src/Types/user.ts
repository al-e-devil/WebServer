import { database } from '../../Proto/database';

export const INITIAL_BALANCE = 1000;

export type User = database.IUser;
export type Transaction = database.ITransaction;
export type Bet = database.IBet;
export type Withdrawal = database.IWithdrawal;
